To try out the different algorithms from Sage:
* be sure to have cloned the ffisom repo (from defeo)
* have a very recent Sage install (at least 5.10.beta7)
* go into this directory (ffisom/sage)
* be sure to have an up to date ellmul module running `git submodule update`
* run "make" from a Sage shell (sage -sh -c make)
* add the current directory to LD_LIBRARY_PATH
* launch Sage
* "import ffisom"
* have fun
